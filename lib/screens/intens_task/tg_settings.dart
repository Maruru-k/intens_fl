import 'package:flutter/material.dart';

class TgSettings extends StatelessWidget {
  const TgSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
