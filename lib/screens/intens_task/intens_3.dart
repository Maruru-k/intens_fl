import 'package:flutter/material.dart';

class IntenseTask3 extends StatelessWidget {
  const IntenseTask3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text(""),
        ),
      ),
    );
  }
}
